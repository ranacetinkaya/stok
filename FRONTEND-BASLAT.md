# 🚀 Frontend Başlatma Rehberi

## Adım 1: Frontend Klasörüne Gidin

```bash
cd frontend
```

## Adım 2: Bağımlılıkları Yükleyin (İlk Kez Çalıştırıyorsanız)

```bash
npm install
```

Bu komut birkaç dakika sürebilir. İlk kez çalıştırıyorsanız mutlaka yapın.

## Adım 3: Frontend'i Başlatın

```bash
npm run dev
```

## ✅ Başarılı Mesaj

Frontend başladığında şunu göreceksiniz:

```
  VITE v5.x.x  ready in xxx ms

  ➜  Local:   http://localhost:3000/
  ➜  Network: use --host to expose
```

## 🌐 Tarayıcıda Açın

Frontend otomatik olarak açılır veya manuel olarak şu adrese gidin:
```
http://localhost:3000
```

## 📝 Notlar

- Frontend `http://localhost:3000` adresinde çalışacak
- Backend'in de çalışıyor olması gerekiyor (`http://localhost:5000`)
- Frontend'i durdurmak için terminal'de `Ctrl + C` tuşlarına basın

## 🔄 Hızlı Başlatma (Tek Komut)

Eğer bağımlılıklar zaten yüklüyse:

```bash
cd frontend && npm run dev
```
