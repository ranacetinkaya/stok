[phases.setup]
nixPkgs = ['python39', 'chromium', 'chromedriver']

[phases.install]
cmds = ['pip install --upgrade pip', 'pip install -r requirements.txt']

[phases.build]
cmds = []

[start]
cmd = 'python app.py'
